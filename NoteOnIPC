Note to self: zombieService in gameViewController is different from previous zombie service. we do have a phantom session.
Fix this then try to transmit again.
Try replacing with 
 let appDelegate = UIApplication.shared.delegate as! AppDelegate
 then
  let zombieService = appDelegate.mpcManager
